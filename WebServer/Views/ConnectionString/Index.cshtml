@model WebServer.Models.ConnectionStringViewModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/_Layout.cshtml";
}

@section styles {
    <link rel="stylesheet" href="~/Content/EZReporting.css" type="text/css" />
}

<input id="CONNECTION_STRINGS_ENDPOINT" type="hidden" value="@Url.Action("GetConnectionStrings", "ConnectionString")" />

<div id="app">

    <div class="col-sm-offest-3 col-sm-6 pull-right">
        <span class="pull-right">
            <a href="@Url.Action("Index", "Home")">Home</a>
        </span>
    </div>
    
    <div class="col-sm-offset-3 col-sm-6 ez-panel">
        <div class="ez-panel-head">
            Create
        </div>
        <div class="ez-panel-body">
            <input id="inpCreateName" type="text" />
            <input id="inpCreateValue" type="text" />
            <button id="btnCreate" type="button" class="btn btnDefault">Create</button>
        </div>
    </div>
    
    <div class="col-sm-offset-3 col-sm-6 ez-panel">
        <div class="ez-panel-head">
            Edit
        </div>
        <div class="ez-panel-body">
            <select id="slctCreate" v-model="connectionStrings"></select>
            <input id="inpEditName" type="text" />
            <input id="inpEditValue" type="text" />
            <button id="btnEdit" type="button" class="btn btnDefault">Create</button>
        </div>
    </div>
    
    <div class="col-sm-offset-3 col-sm-6 ez-panel">
        <div class="ez-panel-head">
            Delete
        </div>
        <div class="ez-panel-body">
            <select id="slctDelete" v-model="connectionStrings"></select>
            <input id="inpDeleteName" type="text" />
            <input id="inpDeleteValue" type="text" />
            <button id="btnDelete" type="button" class="btn btnDefault">Delete</button>
        </div>
    </div>

</div>

@section scripts {
    <script src="~/Scripts/ConnectionString/dist/bundle.js"></script>
}